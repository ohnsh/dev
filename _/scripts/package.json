{
  "name": "scripts",
  "description": "A package to collect binaries by depending on packages that publish them.",
  "private": true,
  "dependencies": {
    "square-mask": "workspace:*",
    "stream-utils": "workspace:*"
  }
}